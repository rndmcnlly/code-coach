// =========================================================================
// Starter code loaded into the editor on first launch
// =========================================================================
export const STARTER_CODE = `// Welcome to Code Coach!
// Start building your Phaser 4 game here.
// The coach will react as you code.

const config = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  scene: {
    preload: preload,
    create: create,
    update: update
  }
};

const game = new Phaser.Game(config);

function preload() {
  // Load your assets here
}

function create() {
  // Set up your game objects here
}

function update() {
  // Game loop logic here
}
`;
